# Portfolio Workspace

The portfolio workspace is a passive-view module for the desktop shell.
It consumes application-layer orchestration results through a presenter and exposes an immutable view model to the UI.
