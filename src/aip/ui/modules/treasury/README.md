# Treasury Workspace

The treasury workspace is a passive-view module for the desktop shell.
It adapts application-facing treasury context into a read-only view model and exposes a lightweight presentation surface for the shell.
