# AIP Enterprise UI shell

This package contains the PySide6 desktop shell foundation for AIP Enterprise.
The shell is intentionally thin and uses the existing application services and container abstractions.
It does not contain domain calculations or business rules.
