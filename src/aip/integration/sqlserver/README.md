# SQL Server connector

This package provides an infrastructure-oriented SQL Server adapter for the existing integration platform. It is deliberately free of business and institution-specific logic and is intended to serve as the reference implementation for future connector adapters.
