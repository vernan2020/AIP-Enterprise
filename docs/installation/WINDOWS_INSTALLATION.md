# Windows installation

AIP Enterprise can be prepared for Windows packaging with PyInstaller.

## Build
```bat
scripts\build_windows.cmd
```

## Verify
```bat
scripts\verify_windows_install.cmd
```

The Linux environment used here validates the packaging configuration only. A Windows executable is not produced in this environment.
